# Seed Curation Run (2026-02-11)

## Objective

Seed the new phage database with an initial Staphylococcus-priority dataset using available MCP integrations.

## MCP Usage

- `supabase` MCP:
  - applied schema migration
  - applied security/performance hardening migration
  - loaded curated seed dataset
  - verified row counts and advisor checks
- `ncbi_datasets_server` MCP:
  - used taxonomy endpoints (`tax_id` 1279, 1280, 1282) to validate host taxonomy context
  - search/genome endpoints were partially unavailable in this environment (multiple 404 responses)
- `pubmedmcp` MCP:
  - server was discoverable but query execution failed in this environment during prior checks

## Seeded Records

- `phages`: 15
- `host_strains`: 3
- `host_range_assays`: 3
- `kinetics_observations`: 3
- `cocktail_experiments`: 1
- `field_citations`: 16

## Source Strategy in This Seed

- Public source records:
  - NCBI Nucleotide RefSeq links (for genome accession-backed phages)
  - Virus-Host DB index for Staphylococcus phage context
  - NCBI taxonomy snapshot for host taxonomic grounding
- Unpublished source records:
  - Partner email observations (`KB824`, `SBP2@2`, `ANB28`) captured with:
    - `evidence.level = unpublished_comm`
    - `confidence = medium`
    - explicit field-level citations

## Notable Caveat

This seed intentionally mixes high-confidence public records with unpublished observations for hypothesis generation. Unpublished entries are tagged `needs_validation`.
